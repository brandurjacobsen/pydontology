---
name: AI Chat Workflow

on:
  issue_comment:
    types:
      - created

permissions:
  contents: read
  issues: write

jobs:
  chat:
    if: ${{ startsWith( github.event.comment.body, '/ai-chat' ) }}
    runs-on: ubuntu-latest

    steps:
      - name: Check collaborator
        uses: actions/github-script@v8
        with:
          script: |
            const checkCollaborator = require('.github/scripts/check-collaborator.js');
            await checkCollaborator({
              github,
              context,
              core,
              username: '${{ github.event.comment.user.login }}',
              requiredPermission: 'write'
            });

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Aider
        run: pip install --upgrade pip && pip install aider-chat

      - name: Get All Comments
        id: comments
        uses: actions/github-script@v8
        with:
          script: |
            const comments = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });
            return comments.data.map(c => `**${c.user.login}** (${c.created_at}):\n${c.body}`).join('\n\n---\n\n');

      - name: AI Chat
        env:
          COMMENT_BODY: ${{ github.event.comment.body }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ALL_COMMENTS: ${{ steps.comments.outputs.result }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          PROMPT="$(cat ./agents/architect-prompt.txt)
          aider \
          --model openrouter/anthropic/claude-sonnet-4.5 \
          --yes \
          --read Architecture.md \
          --file ISSUE_COMMENT_REPLY.md \
          --message "
          ${PROMPT}

          Task:
          Respond to this issue comment in ISSUE_COMMENT_REPLY.md.

          Issue Context:
          Issue #${ISSUE_NUMBER}: ${ISSUE_TITLE}

          Original Issue Description:
          ${ISSUE_BODY}

          Full Comment History:
          ${ALL_COMMENTS}

          Latest Comment to Respond To:
          ${COMMENT_BODY}"

      - name: Reply to Issue Comment
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');
            const reply = fs.readFileSync('./ISSUE_COMMENT_REPLY.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: reply
            })
